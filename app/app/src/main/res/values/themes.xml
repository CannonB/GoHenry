<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.GoHenry" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:fontFamily">sans</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
    <!-- Cold-start splash: centered move icon on the brand background. Swapped to
         Theme.GoHenry in MainActivity.onCreate before Compose draws. -->
    <style name="Theme.GoHenry.Splash" parent="Theme.GoHenry">
        <item name="android:windowBackground">@drawable/splash_background</item>
    </style>
</resources>
